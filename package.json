{
  "name": "ai-project-autopilot",
  "version": "0.1.0",
  "private": true,
  "description": "Prompt-to-PR planning autopilot foundation with Express, JSON storage, generated prompts, dashboard, tests, and CI.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node src/seed.js",
    "test": "node --test tests/*.test.js",
    "ci": "npm run seed && npm test"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "nanoid": "^5.0.7"
  },
  "devDependencies": {}
}
